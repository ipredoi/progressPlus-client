import ProgressList from "./ProgressList";

function RenderProgressList({ progress }) {
  function RenderProgress({ name, role }) {
    return (
      <div>
        <ProgressList />
      </div>
    );
  }
}
export default RenderProgressList;
